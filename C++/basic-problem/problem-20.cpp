#include <iostream>
#include <string.h>
using namespace std;

int main() {
    int t;
    cin>>t;

    while(t--) {
        // string str;
        // cin>>str;
        // string word = strtok(str,",!;?. ");

        // int count = 0;

        // while(word != NULL) {
        //     if(word.length() > 0) {
        //         count++;
        //     }
        // }
    }
    return 0;
}